# Create a render.yaml in your root directory
services:
  - type: web
    name: deepfake-detector
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
